<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{ifset #title}{include #title} | {/ifset}Adminer next generation</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/jquery-datetimepicker/2.5.4/jquery.datetimepicker.css">
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
{*	<link rel="shortcut icon" href="/favicon.ico">
	<link rel="icon" href="/favicon.ico">	*}
</head>
<body>
	<div class="navbar navbar-default navbar-static-top">
		<div class="container-fluid">
			<div class="nav navbar-nav">
				<h2><a href="{link Homepage:default, $presenter->getParam('driver')}">{_'core.title'}</a></h2>
			</div>
			{control breadcrumb}
			<div style="margin: 10px 5px;">
				<div n:if="isset($actualDriver) && $actualDriver" class="pull-right">
					<a class="btn btn-default" href="{link Homepage:logout, $actualDriver->type()}">Logout</a>
				</div>
				<div class="pull-right form-group" style="margin-right: 10px;">
					<select onchange="window.location={link this, 'driver' => $driver, 'locale' => null} + '&locale=' + this.value;" class="form-control">
						<option value="en"{if $locale == 'en'} selected{/if}>English</option>
						<option value="sk"{if $locale == 'sk'} selected{/if}>Slovenčina</option>
					</select>
				</div>
			</div>
		</div>
	</div>

	<div n:if="$flashes" id="flash-alerts">
		<div n:foreach="$flashes as $flash" class="alert alert-{$flash->type}" role="alert">
			<button type="button" class="close" data-dismiss="alert" aria-label="Close">
				<span aria-hidden="true">&times;</span>
			</button>
			{$flash->message|noescape}
		</div>
	</div>

	<script n:if="$flashes">
		setTimeout(function() {
			document.getElementById('flash-alerts').remove();
		}, 2500);
	</script>
	<div class="container-fluid">
		<div class="row">
			{include content}
		</div>
	</div>
	<script src="http://code.jquery.com/jquery-3.1.1.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-datetimepicker/2.5.4/build/jquery.datetimepicker.full.min.js"></script>
	<script>
		$(function(){
			// Datetimepicker initialize
			$('.datetimepicker').datetimepicker({
				format: "Y-m-d H:i:s"
			});
			
			// Datepicker initialize
			$('.datepicker').datetimepicker({
				format: "Y-m-d"
			});
		});
	</script>
</body>
</html>
